# ######################
# .screenrc
# VERSION 1.2
# ######################
#
startup_message off
defscrollback 1000
vbell off
activity "%c activity -> %n%f %t"
crlf off
defmonitor on
shell -$SHELL # loads .bash_profile aliases
termcapinfo xterm ti@:te@ # allow scrolling with xterm

# status line
caption always "%{yK} $USER@%H | %w | "

# clear the scroll buffer
bind / eval "scrollback 0" "scrollback 1000"

screen
